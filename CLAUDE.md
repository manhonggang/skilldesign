# 技能设计 Vault — Project Instructions

## 富文本替换规则

当需要在任何 Markdown 文档中引用游戏术语时，遵循以下规则：

### 1. 查表替换

读取 [[富文本表]]，将所有匹配的术语替换为对应的富文本标签。
使用**精确子串匹配**（`恢复` ≠ `回复`）。

### 2. 反引号包裹（强制）

**每个富文本标签必须用 Markdown 反引号包裹**：

```
施加3层`<color=#f02b2b><href=game://buff/42>燃烧</href></color>`
```

原因：`<color=#...>` 尖括号会被 Obsidian 当作 HTML 解析，导致内容截断或隐藏。反引号强制 Obsidian 将其视为行内代码，完整保留。

复制出库使用时，去掉首尾反引号即可。

### 3. 未收录术语 → 加粗

对于富文本表中**没有**的命名游戏机制（buff/debuff/特殊效果名），使用 `**粗体**`：

- 状态效果：冰冻、压制、死亡预兆、雷殛
- 护盾/buff：烈阳护盾、蝎后护盾、狂怒、怒噬
- 特殊攻击：嗜血蝠、毒翼飓风
- 核心机制：暴击（注意：暴击率在表中，暴击不在）
- 天气/场地：雷暴
- 破甲/减益：破甲、弱化效果

**不加粗**通用术语：伤害、攻击、生命、护盾、回合、目标、敌人、角色、玩家。

### 4. 不动的内容

- `{PerValue1}`、`{FixedValue1}` 等动态占位符 — 永远不改
- `X%` 等变量表达式 — 永远不改

### 5. 行数对应

修改表格时，行数必须与原文严格一一对应，方便复制回原系统。

### 颜色约定（参考）

| 颜色 | 色值 | 用途 |
|------|------|------|
| 红 | `#f02b2b` | 伤害/攻击类 |
| 橙 | `#f07b2b` | 次级/特殊类 |
| 绿 | `#24a422` | 增益/恢复/中毒 |
| 青 | `#229da4` | 冰霜/敏捷类 |
| 紫 | `#6e22a4` | 特殊道具/水晶 |
