---
title: 自动训练天数分布设计
tags: [设计, 概率, 自动训练]
---

# 自动训练天数分布设计

## 1. 设计目标

- 自动训练可到达的**最远天数** = 玩家历史最远训练天数 `M`（不能突破玩家上限）。
- 在 `[1, M]` 内，到达某一天 `d` 的概率服从以 `M` 为中心的**截断正态**（保留左半）。
- 随玩家累计的「自动训练次数 `N`」增加，分布向峰值 `M` 收紧（落在 `M` 的概率提升）。
- 锚点约束：
  - `N = 1`  →  峰值 `P(M) = 20%`
  - `N = 10` →  峰值 `P(M) = 80%`

## 2. 变量定义

| 变量 | 含义 |
|------|------|
| `M` | 玩家历史最远训练天数（自动训练的天花板） |
| `d` | 自动训练实际到达的天数，`d ∈ {1, 2, …, M}` |
| `N` | 玩家累计自动训练次数（≥1） |
| `p(N)` | 目标峰值概率，即 `P(d = M)` |
| `σ` | 正态分布标准差，由 `p(N)` 反解得到 |

## 3. 峰值概率 `p(N)`

`N ∈ [1, 10]` 区间线性插值，两侧截断：

$$
p(N) = \mathrm{clip}\!\left(0.20 + \frac{0.80 - 0.20}{10 - 1}\,(N-1),\ 0.20,\ 0.80\right)
$$

- `N = 1`  →  `0.20`
- `N = 2`  →  `0.267`
- `N = 5`  →  `0.467`
- `N = 10` →  `0.80`
- `N > 10` →  封顶 `0.80`（也可改成继续逼近 `1.0`，按调优需要）

> 想要更陡或更平的收敛，把斜率 `0.6/9` 换成对数形式 `0.20 + 0.60·log(N)/log(10)` 即可，锚点不变。

## 4. 概率分布（半正态，左半保留）

设 `k = M - d ≥ 0`（离天花板的距离）。以 `M` 为中心、截掉右侧后，权重为：

$$
w(d) = \exp\!\left(-\frac{(M-d)^2}{2\sigma^2}\right) = \exp\!\left(-\frac{k^2}{2\sigma^2}\right)
$$

归一化：

$$
P(d) = \frac{w(d)}{Z},\qquad Z = \sum_{k=0}^{M-1}\exp\!\left(-\frac{k^2}{2\sigma^2}\right)
$$

峰值在 `d = M`（即 `k = 0`）：

$$
P(M) = \frac{1}{Z}
$$

## 5. 由 `p(N)` 反解 `σ`

令 `P(M) = p(N)`，即：

$$
Z(\sigma) = \frac{1}{p(N)}
$$

`Z(σ)` 随 `σ` 单调递增（`σ→0` 时 `Z→1`，即峰值→100%；`σ→∞` 时 `Z→M`，即接近均匀）。所以用**二分法**在 `σ ∈ (0, 10M]` 内解一次即可，5~6 次迭代就收敛。

> 这是按玩家 `M` 自适应的——不同玩家天花板不同，曲线自动按比例缩放。

## 6. 配图（以 `M = 20` 为例）

![[attachments/auto_train_dist.png]]

左图：不同 `N` 下的 `P(d)` 曲线，可见随 `N` 增大峰值抬高、左侧尾部变薄。
右图：峰值概率随 `N` 的曲线，线性插值满足 `20%@1` 与 `80%@10` 两个锚点。

`M = 20` 下各档对应的 `σ` 参考值：

| N | 目标峰值 p(N) | 反解 σ | 实际 P(M) |
|---|---|---|---|
| 1  | 0.200 | 3.59 | 0.2000 |
| 2  | 0.267 | 2.59 | 0.2667 |
| 3  | 0.333 | 1.99 | 0.3333 |
| 5  | 0.467 | 1.31 | 0.4667 |
| 10 | 0.800 | 0.60 | 0.8000 |

## 7. 实现伪代码

```python
import math

def peak_target(N):
    return min(0.80, max(0.20, 0.20 + (0.80 - 0.20) * (N - 1) / 9))

def Z_of_sigma(sigma, M):
    s = 0.0
    for k in range(M):                 # k = M-d, 0..M-1
        s += math.exp(-(k*k) / (2*sigma*sigma))
    return s

def sigma_for_peak(p, M):
    target_Z = 1.0 / p
    lo, hi = 1e-3, 10.0 * M
    for _ in range(60):
        mid = (lo + hi) * 0.5
        if Z_of_sigma(mid, M) < target_Z:
            lo = mid
        else:
            hi = mid
    return (lo + hi) * 0.5

def auto_train_dist(M, N):
    p = peak_target(N)
    sigma = sigma_for_peak(p, M)
    Z = Z_of_sigma(sigma, M)
    return [math.exp(-((M-d)**2)/(2*sigma*sigma)) / Z for d in range(1, M+1)]

# 抽样: 按分布随机一个 d
def sample_auto_train_day(M, N, rng):
    probs = auto_train_dist(M, N)
    r = rng.random() * sum(probs)
    acc = 0.0
    for d, pr in zip(range(1, M+1), probs):
        acc += pr
        if r <= acc:
            return d
    return M
```

## 8. 边界与设计注意

- **玩家 `M` 很小（例如 `M = 2`）时**：`N=1` 目标 `0.20` 仍可解（`σ` 较大，接近均匀）；`N=10` 目标 `0.80` 同样可解。分布始终自洽。
- **`N > 10`**：默认封顶 `0.80`。若希望"练得越多越稳"，可把封顶改成 `0.95` 或继续逼近 `1.0`，但 `0.80` 已足够保证仍存在变化感。
- **为什么用截断正态而不是几何衰减**：正态两侧衰减更平滑，避免"早期天数概率断崖式下降"的体验；同时左右对称的形状天然契合"以最远天数为均值"的心智模型。若想要闭式实现，可退化为几何形式 `P(d) = p·r^(M-d)`，`r ≈ 1-p`，效果近似。
- **取整到天**：公式天然按天离散，无需再四舍五入。

## 9. 一句话总结

> 以玩家最远训练天 `M` 为均值的左半正态；用 `p(N)` 线性插值（1→20%、10→80%）定峰值，再由 `P(M)=1/Z` 反解 `σ`。`N` 越大、`σ` 越小、越集中在 `M`。
